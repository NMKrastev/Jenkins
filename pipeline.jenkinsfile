pipeline {

    agent any 

    stages {
        stage ('Build') {
            steps {
                sh "ls"
                sh "pwd"
                sh """
                    cd Jenkins/src
                    ls
                    javac HelloWorld.java
                """
                input message: 'Need some input'
            }
        }
    }
    
    post {
        always {
            echo 'I will always say Hello again!'
        }
    }
}
